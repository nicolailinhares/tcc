<%= form_for(@notificacao) do |f| %>
  <% if @notificacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notificacao.errors.count, "error") %> prohibited this notificacao from being saved:</h2>

      <ul>
      <% @notificacao.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao_falha, "Descrição do pedido" %><br />
    <%= f.text_area :descricao_falha, :size => "40x10" %>
  </div>
  <div class="field">
    <%= label_tag 'status', "Estado do equipamento" %><br />
    <%= select_tag 'status', options_for_select(@opcoes_status,@selecionado) %>
  </div>
  <%=f.hidden_field :instituicao_id %>
  <%=f.hidden_field :setor_id %>
  <%=f.hidden_field :item_id %>
  <%=f.hidden_field :usuario_id %>
  <%=f.hidden_field :data_abertura %>
  <div class="actions">
    <%= f.submit 'Criar'%>
  </div>
<% end %>
