<h1>Item</h1>

<%= render 'form' %>

<%= link_to 'Info', instituicao_setor_item_path(@instituicao.id,@setor.id,@item.id) %> |
<%= link_to 'Voltar', instituicao_setor_path(@instituicao.id,@setor.id) %>
